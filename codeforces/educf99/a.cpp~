#include <iostream>
#include <vector>

using namespace std;

int main() {
    int t;
    cin >> t;

    while (t--) {
        int n;
        int cnt = 0;
        cin >> n;
        while (n /= 10) {
            cnt++;
        }
        cout << cnt << endl;
    }
    return 0;
}
